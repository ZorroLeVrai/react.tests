# Example using the React Testing Library

This application is only used as an example on how to use the React Testing Library.

To add a test configuration, add a `setupTest.js` file in the /src directory.
